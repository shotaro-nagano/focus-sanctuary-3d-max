# VISUAL_QA.md — Focus Sanctuary · 3D MAX EDITION（CHRONO CHRYSALIS / CLEFT VAULT）

記入ルール: 「実施方法」は **ブラウザ実操作（実 GPU）／Playwright 録画・静止画（実 GPU）／自動 UI テスト（Playwright）／ユニットテスト／コード読解** を区別して書く。未実施は未実施と書く。静止画だけで動きを確認したとはしない。

## 0. 確認環境

| 項目 | 内容 |
| --- | --- |
| OS | Windows 11 Home 10.0.26200 |
| GPU / WebGL | Intel Iris Xe Graphics (0x9A49) — ANGLE D3D11, WebGL 2.0 (OpenGL ES 3.0 Chromium), float テクスチャ可 |
| ブラウザ | Chromium 1223（Playwright 同梱、ヘッド付き＝実 GPU）。補助: Claude Code アプリ内ブラウザ（Chromium、WebGL2 で動作確認） |
| 画面サイズ | PC 1440×900（DPR 1、一部 DPR 2）、モバイル 390×844（DPR 2、タッチ・モバイル UA エミュレーション） |
| 品質プリセット | PC: `high`（既定）、モバイル: `medium`（既定）。比較時は同じ URL・同じプリセットを使うこと |
| ビルド | Node 18.13.0 / npm 8.19.3 / three 0.185.1 / gsap 3.15.0 / vite 6.4.3 / typescript 5.9.3 / vitest 3.2.7 |
| 確認日 | 2026-09-06 〜 2026-09-07 |

証跡ファイル: `captures/desktop-*.png`、`captures/mobile-*.png`（各ポーズの静止画）、`captures/demo-desktop.webm`、`captures/demo-mobile.webm`（デモ動画）、`captures/qa/*`（確認用の連続キャプチャのコンタクトシートと診断静止画）。

## 1. M01〜M12 実装状況

| ID | 演出 | 実装 | 実装場所 / 確認 |
| --- | --- | --- | --- |
| M01 | シネマティックな登場 | 実装済 | `motion/Director.buildIntro`: シーム唇のマクロ（dolly 1.15, fov 28, 環境マップ回転で白い帯が滑る）→ 0.5〜2.2 s ドリーアウト・殻の開き（back.out）・コア点灯 → 1.0 s FOCUS がマスクから出現、1.4 s SANCTUARY スライド → 1.8 s 軌道飛来・数字組み上げ・コンソール整列 → 3.0 s 待機。REPLAY で同タイムラインを再生し現在のポーズへ復帰。録画 `captures/qa/intro3/intro3-sheet.png` で中間フレーム確認 |
| M02 | 浮遊と複合軌道 | 実装済 | 自転 90 s、浮遊 7 s／11 s、軌道 3 本は周期 41／67／29 s・別軸・別位相、コアリング逆回転、粒子渦 20 s。`Stage.render` は `sceneTime`（`timeScale` 累積）で駆動 |
| M03 | 表面の生きた変化 | 実装済 | 頂点シェーダー: シームのねじれ（`uTwist`）＋ 3D ノイズによる液体的なうねり（`uLiquidAmp`×`uBreath`、法線補正）。サッシュは `sin(9s+1.7t)` の膨らみ。環境マップ回転 0.5°/s ドリフト |
| M04 | ポインターへの反応 | 実装済 | カメラ視差 yaw ±3°・pitch ±1.5°・注視点 ±0.08、環境マップ ±6°回転、前景粒子の視差。`Director.update` の減衰（λ=6）。録画 `captures/qa/interact/interact-sheet.png` |
| M05 | ドラッグによる鑑賞 | 実装済 | 彫刻ルートを yaw ±40°／pitch ±15° に制限して回転、離すとスプリングで復帰。`[data-ui]` 要素上では開始しない。同上録画で確認 |
| M06 | モード間のモーフィング | 実装済 | `FORM_POSES`＋`CAMERA_POSES` を 1.2〜1.6 s で連続補間（殻の開き・ねじれ・扉・サッシュ・コア色・軌道整列・カメラ回り込み・環境回転）。単語入替 FOCUS→BREATHE→RELEASE。録画 `captures/qa/morph2/morph2-sheet.png` |
| M07 | 集中開始の起動演出 | 実装済 | `onStart`: 集中ポーズへ ＋ コア発光スパイク、RectAreaLight のスイープ（`lightSlide` −1→1）、殻の呼吸パルス、軌道整列 back.out。録画 `captures/qa/pause/pause-sheet.png` 4.0〜4.7 s |
| M08 | 一時停止の時間凍結 | 実装済 | `timeScale` 1→0（1.4 s）で全周期運動・粒子・脈動が位相を保って減速停止、うねり停止、減彩 0.25・ビネット強化・リム強調、カメラが 6 s かけて僅かに寄る。同上録画 7〜11 s |
| M09 | 再開時の再加速 | 実装済 | 同じ `sceneTime` から `timeScale` 0→1（0.9 s）、照明・減彩を復元。跳ねなし（同上録画 11 s〜） |
| M10 | 完了時の大きな見せ場 | 実装済 | 0〜0.6 s 収束（粒子引力、殻が初めて完全に閉じる、コア圧縮、進捗アーク消滅）→ 0.6〜1.8 s 解放（ねじれ逆転→花弁バースト 1.35、扉 90°、ハブ持ち上げ、キール展開、サッシュ展開、衝撃波リング、カメラ引き＋50°回り込み）→ 1.2〜2.6 s FOCUS バンドワイプ、SESSION／COMPLETE、数字再形成 → 2.2〜4.0 s 弾性で休憩ポーズへ再構成。録画 `captures/qa/complete3/complete3-sheet.png` |
| M11 | 磁力を感じる操作反応 | 実装済 | `ui/magnetic.ts`: 24 px 以内でポインターに吸い寄せ・スプリング復帰、押下で縮小とグラデーション移動。押下時に `Director.onPress`（殻の呼吸パルス、光の揺れ）。カーソルは DRAG／PRESS ラベル付き（`captures/qa/stress-after.png` にラベル表示） |
| M12 | キネティックタイポグラフィ | 実装済 | `ui/typography.ts`: 文字単位のマスク出現（skew 付き）、単語入替、数字の組み上がり、M10 の 3 バンドワイプ。FOCUS はキャンバスの背後（彫刻が実際に隠す）、SANCTUARY は前景（screen 合成） |

## 2. 視覚確認 V01〜V15

| ID | 確認内容 | 実施方法 | 結果 | 備考 |
| --- | --- | --- | --- | --- |
| V01 | 本物の3D | ブラウザ実操作＋Playwright 録画（実 GPU） | 合格 | WebGL2 で描画（`?fps=1` に約 90 コール・約 20 万三角形）。ドラッグ・視差で奥行きが変わる。透過ガラスは three の transmission バッファ |
| V02 | 独自の輪郭 | 静止画（実 GPU）`captures/desktop-idle.png` ほか | 合格 | 螺旋シームの五枚花弁、斜めのクレフト、赤道スリット、極のハブ、欠けた軌道の断片。グレー素材でも形が読める（開発中に確認） |
| V03 | 素材の違い | 静止画 | 合格 | 暗い鏡面クロームに白い帯の反射、内部のガラスサッシュは縁が光り背後が透ける、コアは発光してブルーム。キールはブラッシュド（異方性） |
| V04 | 光と暗部 | 静止画・診断キャプチャ | 合格（調整後） | 初期実装は環境光・RectAreaLight・ブルームが強すぎて白飛び→環境 HDR 値、roughness、コア光、ブルーム閾値 1.25 に調整。数字や輪郭は飛ばない |
| V05 | 登場演出 | 録画 `intro3-sheet.png` | 合格 | マクロ→引き→文字→軌道→数字の順で 1 シーケンス。開発サーバーでは初回読込にモジュール変換の時間があるため、登場の前に黒画面が続く（ビルド版は約 1 s） |
| V06 | 待機中の運動 | 録画（10 s 以上）`interact-sheet.png` 8〜12 s | 合格 | 自転・浮遊・軌道・リング・粒子が別周期で動く。全体回転だけではない |
| V07 | ポインターとドラッグ | 録画 `interact-sheet.png` | 合格 | 視差で反射が滑り、ドラッグで回転し離すとスプリングで戻る |
| V08 | モード間の形状変化 | 録画 `morph2-sheet.png` | 合格 | 花弁が滑り出る途中・扉が開く途中・サッシュが出る途中の姿が見える。色替えだけではない |
| V09 | 開始・停止・再開 | 録画 `pause-sheet.png`＋自動 UI テスト F01 | 合格 | 起動演出→運動→停止で凍結→再開で同位相から再加速。数字は連続 |
| V10 | 完了演出 | 録画 `complete3-sheet.png`、`captures/demo-desktop.webm` 末尾 | 合格 | 収束（閉じる）→展開（花弁バースト）→文字→再構成が確認できる。紙吹雪ではなく彫刻の変形 |
| V11 | キネティックタイポ | 録画 intro／complete | 合格 | FOCUS は彫刻に実際に隠され、SANCTUARY は前を横切る。文字単位マスク・バンドワイプ |
| V12 | PCとモバイル | 静止画 `captures/desktop-*.png` / `captures/mobile-*.png`、`demo-mobile.webm` | 合格 | モバイルは縦組み FOCUS、上部 SANCTUARY、上 60% に彫刻、大きな数字、下部シート。縮小版ではない |
| V13 | 繰り返し操作 | 自動操作（Playwright, 実 GPU）: モード 18 連打・REPLAY 3 回・リサイズ 5 回 | 合格 | 実行後も彫刻・カメラ健在（`captures/qa/stress-after.png`）、ロック解除済、54 fps、エラーなし |
| V14 | 連続動作 | 自動操作 30 s（5 s ごと FPS 採取）＋各録画 | 合格 | 51〜55 fps で安定、点滅・破綻なし |
| V15 | 動きの低減・WebGL失敗 | 静止画 `captures/qa/reduce.png`（`?reduce=1`）、`captures/qa/nogl.png`（Chromium `--disable-3d-apis`） | 合格 | 低減時は代表構図で静止し短い切替のみ。WebGL 不可時は「3D UNAVAILABLE」と CSS 構図に切替、タイマー操作可。3D 成功とは扱わない |

## 3. 基本動作 F01〜F06

実施方法: `python tools/functional_check.py`（Playwright、実 GPU、Chromium 1223）。試験用時計 `?speed=60`（F01/F02/F04/F06）と `?speed=600`（F03: 25 分＝2.5 s）を使用。本番の 25／5／15 分は変更していない。保存先は `?storage=session`（実データに触れない）。**33 / 33 項目合格**。加えて Vitest（`src/timer/engine.test.ts` 58 件、`src/motion/__sanity.test.ts` 12 件）全件合格。

| ID | 確認内容 | 実施方法 | 結果 | 備考 |
| --- | --- | --- | --- | --- |
| F01 | 開始・一時停止・再開 | 自動 UI テスト ×60 | 合格 | 3 s で約 3 分進む（21.9 分残）、停止中は残り時間不変（PAUSED 表示）、再開は停止値から継続 |
| F02 | リセット・モード変更 | 自動 UI テスト | 合格 | 実行中のリセット／モード変更は確認ダイアログ、KEEP で継続（同一セッション ID）、DISCARD で記録なしに 05:00 待機、待機中の変更は確認なし |
| F03 | 集中と休憩の完了 | 自動 UI テスト ×600 | 合格 | 4 サイクル: 集中だけ加算（25/50/75/100 分）、次モードは待機、休憩は非加算、4 回目で長い休憩、セッション ID 一意 |
| F04 | 再読み込み・日付変更・復帰 | 自動 UI テスト＋ユニットテスト | 合格 | 実行中／停止中の復元、放置中に終了したセッションは復帰時に 1 回だけ完了（再読込でも二重計上なし）。日付変更・深夜跨ぎはユニットテストで検証（実ブラウザでは試験用時計が 1 日を跨いだ際に当日記録が正しく 0 へ更新されることを観察） |
| F05 | 作業・保存失敗 | 自動 UI テスト | 合格 | `<b>…<img onerror>` を含む作業名は文字列として保持・表示。localStorage アクセスが例外を投げる状態で起動→通知表示、タイマーはメモリで動作 |
| F06 | REPLAYと完了プレビュー | 自動 UI テスト | 合格 | REPLAY 中もタイマーは実時間どおり進み記録は不変。`?preview=complete` は記録 0 のまま |

## 4. 視覚改善の記録（確認→修正→再確認）

1. **白飛び・乳白色の殻**（初回統合時）: 環境マップの HDR 値（帯 3〜5.6）・RectAreaLight 8・クローム roughness 0.12・コア発光 1.5 倍・ブルーム 0.55 が重なり、殻が乳白色に透けて見えた。→ 帯を 1.0〜2.4 に、ドーム輝度半減、roughness 0.045（ノイズ 0.025〜0.045）、RectAreaLight 0.9（8×0.16）、コア発光・点光源を半減、ブルーム 0.38／半径 0.42／閾値 1.25、露出 1.0。再確認 `captures/qa/v-q-idle.png`。
2. **短い休憩で画面中央が白飛び**: `pow(負数, 2.0)` による NaN がブルームで全面に広がっていた（シーム光と環境ドームの 2 箇所）。→ 二乗を明示。さらにブルーム入力に NaN/Inf 除去と 24 のクランプを追加（`scene/post.ts`）。再確認 `captures/qa/v-q-short.png`。
3. **完了演出の収束で全面フラッシュ**: 粒子が 1 点に集まり加算合成で白飛び。→ 引力の最小半径 0.3、引力中の粒子アルファ 40% に。再確認 `complete3-sheet.png`。
4. **登場演出のマクロが灰色のもや**: カメラが殻の内側に入っていた。→ シーム唇の外側 1.15 units、fov 28、コア光を絞りブルームを低く始める。再確認 `intro3-sheet.png`。
5. **登場後に FOCUS が消える**: 文字分割後の子要素に `background-clip: text` が届かず透明になっていた。→ 各文字にグラデーションを付与。
6. **SESSION／COMPLETE の重なり、完了中の数字の衝突**: 行間とバナー位置を調整し、バナー表示中は浮遊数字を退避。
7. **モバイルの数字がコンソール裏に隠れる**: 投影アンカーをやめ、彫刻ゾーンとコンソールの間の帯に固定。再確認 `captures/qa/m-idle2.png`。
8. **集中ポーズが緑のもや**: コア発光 2.4→1.5、点光源係数 1.4→1.1、シーム光を弱めた。再確認 `captures/qa/v-r-focus.png`。
9. **性能**: 1440×900・DPR 2 の `high`（当初は床反射・フル解像度 transmission）で 21 fps。→ `high` は DPR 上限 1.5・transmission 半解像度・床反射なし、床反射入りは `ultra` に分離。粒子 4,760 個。DPR 1 で 52〜60 fps。
10. **フレームバッファ警告**: 起動直後の 2 フレームで「Attachment has zero size」→ リサイズ前は描画しないガードを追加（`Stage.sized`）。

## 5. 未実施項目・既知の問題

- **60 fps**: 実測は DPR 1 の 1440×900 `high` で 52〜60 fps、DPR 2 では約 34 fps（Intel 内蔵 GPU）。強い GPU では未計測。高 DPI モニタでは `high` が DPR 1.5 に制限される。
- **実機モバイル**: 未実施。390×844 はデスクトップ Chromium のタッチ・モバイル UA エミュレーション（DPR 2）で確認。
- **ガラス同士の屈折**: three.js の transmission の仕様上、透過体は不透明バッファのみを見る（互いを屈折させない）。`low` プリセットのガラスは近似（背景の歪みなし）。
- **登場演出の前の黒画面**: 開発サーバーでは初回読み込みにモジュール変換の時間がかかる（1〜10 s）。ビルド版では約 1 s。読み込みを遅らせる偽のプログレスは入れていない。
- **Firefox / Safari**: 未確認（Chromium のみ）。
- **完了音**: 未実装（任意項目）。
- **ヘッドレス（SwiftShader）**: 時間依存の検証には不向き（描画が遅く JS が詰まる）。`tools/functional_check.py` は既定でヘッド付き・実 GPU。
- 開発中、ヘッド付き Chromium のウィンドウが手動で閉じられると Playwright の検証が中断する（実際に 2 回発生）。再実行で 33/33 合格。
